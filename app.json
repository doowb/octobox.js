{
  "name": "Octobox.js",
  "description": "Take back control of your GitHub Notifications",
  "repository": "https://github.com/doowb/octobox.js",
  "env": {
    "URL": {
      "description": "Base URL for the site being created. Example: https://octoboxjs.herokuapp.com",
      "required": true
    },
    "GITHUB_CLIENT_ID": {
      "description": "The GitHub Application Client ID",
      "required": true
    },
    "GITHUB_CLIENT_SECRET": {
      "description": "The GitHub Application Client Secret",
      "required": true
    },
    "GA_TRACKING_ID": {
      "description": "Google Analytics tracking ID. This will include Google Analytics scripts in each page.",
      "required": false
    }
  },
  "addons": [
    "heroku-postgresql"
  ]
}
